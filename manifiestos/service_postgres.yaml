apiVersion: v1
kind: Service
metadata:
  name: service-postgres
  namespace: miniorders
spec:
  selector:
      deployment-postgres : label-deploy-postgres

  ports:
    - port: 5432
      targetPort: 5432